[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "npm run start:backend"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"

[service]
root = "src/codes/backend"
